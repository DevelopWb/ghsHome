<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:paddingBottom="5dp"
    android:layout_height="wrap_content"

    >

    <TextView
        android:id="@+id/visitor_msg_key_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|left"
        android:maxWidth="100dp"
        android:singleLine="true"
        android:ellipsize="end"
        android:text="费用项目"
        android:textColor="@color/gray_deeper"
        android:textSize="16dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/visitor_msg_value_tv"
        android:layout_width="0dp"
        app:layout_constraintTop_toTopOf="@+id/visitor_msg_key_tv"
        app:layout_constraintBottom_toBottomOf="@+id/visitor_msg_key_tv"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|left"
        android:text="费用项目"
        android:textColor="#333333"
        android:textSize="16dp"
        app:layout_constraintLeft_toRightOf="@+id/visitor_msg_key_tv"
        app:layout_constraintRight_toRightOf="parent"
         />


</android.support.constraint.ConstraintLayout>